{
  "name": "0auth-example-server",
  "version": "1.0.0",
  "main": "./index",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "yarn clean && tsc && node build",
    "clean": "rm -rf ./build && rm -rf tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@0auth/message": "^1.0.8",
    "@0auth/server": "^1.0.8",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.7",
    "@types/express": "^4.17.7",
    "@types/node": "^14.6.2",
    "typescript": "^4.0.2"
  }
}
